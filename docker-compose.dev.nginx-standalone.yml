---
version: "3.8"

services:
  nginx:
    image: nginx
    depends_on:
      - django
    ports:
      - "${PROJECT_PORT_EXTERNAL}:80"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d
      - ./app/staticfiles:/staticfiles:ro
      - ./app/media:/media:ro
